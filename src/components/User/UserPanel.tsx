import AddUserPanel from './AddUserPanel';
import UserList from './UserList';

const UserPanel = () => {
  return (
    <>
      <AddUserPanel />
      <UserList />
    </>
  );
};

export default UserPanel;
